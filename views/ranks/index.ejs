<h1><%= title %></h1>
<div>
    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Nombre del Rango</th>
                <th>Fecha de Creación</th>
                <th>Fecha de Edición</th>
            </tr>
        </thead>
        <tbody>
            <% ranks.forEach((rank, index) => { %>
            <tr>
                <td><%= index + 1 %></td>
                <td><%= rank.nameRank %></td>
                <td><%= rank.createdAtRank %></td>
                <td><%= rank.writedAtRank %></td>
            </tr>
            <% }) %>
        </tbody>
    </table>
</div>
